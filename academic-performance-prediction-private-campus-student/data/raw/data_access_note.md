# Data access note

The original Google Form response file is kept by the group.

The public repository may only include the cleaned dataset because the raw file is part of the survey collection record.
